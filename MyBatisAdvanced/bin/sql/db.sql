CMD > sqlplus conn as sysdba

[SQLPLUS] 
create user mybatis2 identified by mybatis2;
grant resource, connect to mybatis2;

CREATE TABLE BLOG (
	BLOG_ID NUMBER PRIMARY KEY,
	BLOG_NAME NVARCHAR2(200),
	BLOG_TITLE NVARCHAR2(2000)
);

ALTER TABLE BLOG ADD BLOG_AUTHOR_ID NUMBER;

CREATE SEQUENCE SEQ_BLOG;

INSERT INTO BLOG VALUES(SEQ_BLOG.NEXTVAL, '블로그명', '블로그제목', 1);
SELECT * FROM BLOG;

CREATE TABLE AUTHOR (
	AUTHOR_ID NUMBER PRIMARY KEY,
	AUTHOR_NAME NVARCHAR2(200),
	BLOG_ID NUMBER,
	FOREIGN KEY (BLOG_ID) REFERENCES BLOG(BLOG_ID)
);

CREATE SEQUENCE SEQ_AUTHOR;

INSERT INTO AUTHOR VALUES (SEQ_AUTHOR.NEXTVAL, '작성자명', 1);
INSERT INTO AUTHOR VALUES (SEQ_AUTHOR.NEXTVAL, '작성자명', 2);
INSERT INTO AUTHOR VALUES (SEQ_AUTHOR.NEXTVAL, '작성자명', 3);
TRUNCATE TABLE AUTHOR;
DROP SEQUENCE SEQ_AUTHOR;

SELECT * FROM AUTHOR;

CREATE TABLE COMM(
	COMM_ID NUMBER PRIMARY KEY,
	COMM_TITLE NVARCHAR2(500),
	COMM_CONTENT NVARCHAR2(2000),
	BLOG_ID NUMBER,
	FOREIGN KEY (BLOG_ID) REFERENCES BLOG(BLOG_ID)
);

CREATE SEQUENCE SEQ_COMM;

INSERT INTO COMM VALUES(SEQ_COMM.NEXTVAL, '게시물제목', '게시물내용', 1);
INSERT INTO COMM VALUES(SEQ_COMM.NEXTVAL, '게시물제목', '게시물내용', 2);
SELECT * FROM COMM;

COMMIT;